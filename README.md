# Senler OAuth Integration Example

Минималистичный пример интеграции с Senler API через OAuth 2.0 авторизацию.

## ⚡ Быстрый запуск

```bash
# 1. Установка зависимостей
npm install

# 2. Настройка переменных окружения
cp env.example .env
# Заполните SENLER_CLIENT_ID и SENLER_CLIENT_SECRET

# 3. Запуск
npm start
```

Приложение будет доступно на `http://localhost:3000`

## 🔧 Настройка Senler приложения

1. Перейдите в [панель разработчика Senler](https://senler.ru/integration/settings/)
2. Создайте новое приложение
3. Укажите **Callback URL**: `http://localhost:3000/auth/senler/callback`
4. Скопируйте `Client ID` и `Client Secret` в `.env`

## 📱 Режимы авторизации

### Popup (рекомендуется)
```env
AUTH_MODE=popup
```
✅ **Работает в iframe интеграции**

### Redirect (ограниченная поддержка)
```env
AUTH_MODE=redirect
```
⚠️ **НЕ работает в iframe** (будет поддержано позже)

## 🌐 API Endpoints

- `GET /` - главная страница
- `GET /auth/senler` - начало OAuth авторизации
- `GET /auth/senler/callback` - OAuth callback
- `POST /auth/subscribers` - получение статистики подписчиков
- `POST /auth/logout` - выход из системы

## 🖼️ Использование в iframe

```html
<iframe 
  src="http://localhost:3000?group_id=YOUR_GROUP_ID" 
  width="800" 
  height="600">
</iframe>
```

**Автоматически работает без дополнительных настроек!**

## ⚙️ Переменные окружения

```env
# Обязательные
SENLER_CLIENT_ID=your_client_id
SENLER_CLIENT_SECRET=your_client_secret
SENLER_CALLBACK_URL=http://localhost:3000/auth/senler/callback

# Опциональные
PORT=3000
AUTH_MODE=popup  # popup или redirect
SESSION_SECRET=your_random_secret
SENLER_API_URL=https://api.senler.ru/v2/
```

## 📁 Структура проекта

```
src/
├── app.ts              # Главный файл приложения
├── config/passport.ts  # Настройка OAuth
├── routes/
│   ├── index.ts        # Главная страница
│   └── auth.ts         # Авторизация и API
├── services/senler.ts  # Сервис для Senler API
└── utils/html.ts       # HTML шаблоны

public/
├── css/                # Стили
└── js/                 # Клиентский JavaScript
```

## 🔐 Безопасность

- **localStorage** для хранения токенов (работает в iframe)
- **Токены валидируются** на сервере при каждом API запросе
- **CORS** настроен для безопасного API доступа

## 🧪 Тестирование

```bash
# Запуск сервера
npm start

# Откройте http://localhost:3000
# Нажмите "Войти через Senler"
# Проверьте localStorage в DevTools
```

## 📚 Используемые библиотеки

- **express** - веб-сервер
- **passport-senler** - OAuth авторизация
- **senler-sdk** - работа с Senler API
- **typescript** - типизация

---

**Готов к использованию в production и iframe интеграции!** 🎯